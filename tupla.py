# As tuplas são quase iguais as listas, só que, uma vez criadas
# não podem ser alteradas, ou seja, não pode remover, adicionar, alterar
#Pode-se criar várias tuplas em uma variável

# A única diferença de sua criação das listas, é que tem que usar os parênteses()
# invés de usar colchetes[], e também, se quiser, NÃO precisa usar colchetes[]

primeiraTupla = ("João", 18, False, 155.43)
print(primeiraTupla)


segundaTupla = 'Maria', 23, 112.56, False
print(segundaTupla)



# Para acessar um item específico é a mesma coisa que as listas
# acesse usando colchetes, e pelo índice, print(nomeTupla[índice])

print(primeiraTupla[2])